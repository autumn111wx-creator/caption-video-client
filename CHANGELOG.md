# 版本记录

## v3.0.0-beta.1 — 2026-09-15

首个 GitHub 归档版本，对应桌面客户端试用版 3.0。

- 改为 Windows 独立窗口，加入托盘、状态栏及退出入口。
- 新增可选视频背景，不选视频时默认纯黑背景。
- 支持 TXT/SRT 导入，按配音重新生成字幕时间轴。
- 视频输出 MP4、MOV、MKV、WebM、AVI；音频输出 WAV、MP3、M4A、FLAC、OGG。
- 倍速范围扩展为 0.5–2.0，步长 0.1。
- 音色 JSON 配置放在指定目录，重启识别。

### 验证记录

制作时已验证黑底和自选视频两条生成路径、0.5/2.0 倍速、各输出格式转码、桌面包启动及通过退出接口结束进程。自选 1.5 秒测试视频成功循环并叠加字幕。原生托盘右键菜单和文件选择对话框仍建议在实际试用中检查；这次上传不将其宣称为完整人工验收。

### 已知限制

背景视频原声替换为新配音；不提供语音自动识别；输入 SRT 原时间码不保留；输出固定 1920×1080 且居中裁切。在线配音受服务可用性影响。
